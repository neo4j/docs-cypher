= Composed queries

Cypher contains operators that enable the composition of separate query branches.
These operators manage the logic and execution flow of queries and, as such, cannot be used as regular clauses.

For more information, see:

* xref:queries/composed-queries/combined-queries.adoc[]
* xref:queries/composed-queries/conditional-queries.adoc[] label:new[Introduced in Neo4j 2025.02]
