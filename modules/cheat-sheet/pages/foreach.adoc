= link:{neo4j-docs-base-uri}/cypher-manual/{page-version}/clauses/foreach/[FOREACH^]



====
include::ROOT:clauses/foreach.adoc[tag=clauses_foreach_node]

[.description]
`FOREACH` can be used to update data, such as executing update commands on elements in a path, or on a list created by aggregation.
This example sets the property `marked` to `true` on all nodes along a path.
====

====
include::ROOT:clauses/foreach.adoc[tag=clauses_foreach_relationship]

[.description]
This example sets the property `marked` to `true` on all relationships along a path.
====

====
include::ROOT:clauses/foreach.adoc[tag=clauses_foreach_create]

[.description]
This example creates a new node for each label in a list.
====